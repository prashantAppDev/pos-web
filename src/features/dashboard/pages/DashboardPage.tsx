import { Typography } from 'antd';

const { Title, Text } = Typography;

export const DashboardPage = () => (
  <div>
    <Title level={4} style={{ margin: 0, marginBottom: 8 }}>Dashboard</Title>
    <Text type="secondary">Coming soon.</Text>
  </div>
);